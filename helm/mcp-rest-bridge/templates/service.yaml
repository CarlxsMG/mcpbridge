apiVersion: v1
kind: Service
metadata:
  name: {{ include "mcp-rest-bridge.fullname" . }}
  labels:
    {{- include "mcp-rest-bridge.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mcp-rest-bridge.selectorLabels" . | nindent 4 }}
