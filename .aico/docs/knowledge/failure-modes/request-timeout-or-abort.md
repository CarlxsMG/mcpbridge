---
id: failure_mode_8c4c9b916c8ed556
kind: failure_mode
source_path: request timeout or abort
title: "Request Timeout or Abort"
run_id: 9e628494-d1e3-4ccc-a704-a37300cef0ce
generated_at: 2026-05-07T21:09:04.701Z
---

# Request Timeout or Abort

**Path:** `request timeout or abort`  
**Kind:** `failure_mode`

> AbortSignal.timeout(effectiveTimeout) fires or abortClientRequests() is called externally. The fetch throws, breaker.recordFailure() is called, and the error is returned as isError.

AbortSignal.timeout(effectiveTimeout) fires or abortClientRequests() is called externally. The fetch throws, breaker.recordFailure() is called, and the error is returned as isError.



