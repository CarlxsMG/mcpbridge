openapi: "3.1.0"
info:
  title: Test API YAML
  version: "1.0.0"
servers:
  - url: /api/v1
paths:
  /items:
    get:
      operationId: list-items
      summary: List items
      responses:
        "200":
          description: Success
